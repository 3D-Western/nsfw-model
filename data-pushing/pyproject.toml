[project]
name = "data-pushing"
version = "0.1.0"
description = "Raw STL ingest uploader and JSONL shard writer"
requires-python = ">=3.11"
dependencies = [
  "boto3>=1.34.0",
  "ulid-py>=1.1.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
